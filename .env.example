# Mapbox public access token (starts with "pk."). Set it in Vercel under
# Project Settings -> Environment Variables. It's read at build time, so
# redeploy after changing it.
NEXT_PUBLIC_MAPBOX_TOKEN=
